import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'rubik.s.race',
  appName: "Rubik's Race",
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
